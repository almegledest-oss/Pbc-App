import React from 'react';

// Floating controls disabled to prevent obscuring content, relying on clean top bar navigation
export const FloatingBackHomeControls: React.FC = () => {
  return null;
};

