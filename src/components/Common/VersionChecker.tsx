import React from 'react';
export { AppUpdateSettingCard } from './AppUpdateSettingCard';

/**
 * Legacy placeholder to prevent any dangling imports.
 * Background version management is now gracefully handled by VersionProvider in VersionContext.
 */
export const VersionChecker: React.FC = () => {
  return null;
};
